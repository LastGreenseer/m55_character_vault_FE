import React from "react";
import styled from "styled-components";

const StyledFooter = styled.footer`
  text-align: center;
 
`;

const Footer = () => {
  return <StyledFooter>M55-Final-Project</StyledFooter>;
};

export default Footer;
