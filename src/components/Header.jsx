import React from "react";
import styled from "styled-components";

const Header = () => {
  return <StyledHeader>Character Vault</StyledHeader>;
};

const StyledHeader = styled.h1`
    text-align: center;
`

export default Header;
